# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright e2e artifacts (npm run test:e2e)
/test-results/
/playwright-report/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Live-browser harness identity marker (scripts/lib/harnessProvenance.mjs) — written into
# public/ for the length of one provenance check and deleted again; only ever visible here if a
# harness run was killed mid-check.
/public/.strides-harness-identity.json
