# adopt-spectral-vo-cm-estimator

Swap the scale-calibrated centimetre vertical-oscillation calculation's amplitude estimator from
per-run extrema pairing to the shared spectral sinusoid fit, so camera-approach drift is absorbed
by the fit's trend terms instead of being charged to the bounce.
